# datasets/

HuggingFace datasets stored in Arrow format. Downloaded via `python tools/download_hf_dataset.py --name <dataset>` or programmatically with `common.data.download_dataset()`.

Datasets are organized by HuggingFace namespace (e.g., `roneneldan/TinyStories`, `nampdn-ai/tiny-textbooks`).
