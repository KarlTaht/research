# models/

Model checkpoints (`.pt` files) and custom tokenizers. Checkpoints are saved during training with naming convention `<model>_<dataset>_epoch_<n>.pt`.

Subdirectories:
- `tokenizers/` - Custom trained tokenizers
